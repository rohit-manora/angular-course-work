import { ScrollListernerDirective } from './scroll-listerner.directive';

describe('ScrollListernerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollListernerDirective();
    expect(directive).toBeTruthy();
  });
});
